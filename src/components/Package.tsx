"use client";

// Asymmetric type sprawl — placeholder words while Flavio decides what
// actually belongs here. Stripped of all counts, numbering, and logistics.
export function Package() {
  return (
    <section
      id="package"
      className="relative min-h-[80vh] sm:min-h-[90vh] bg-cream text-text-dark overflow-hidden"
    >
      <div className="relative w-full max-w-7xl mx-auto h-full min-h-[80vh] sm:min-h-[90vh] px-6 sm:px-12 py-20">
        {/* Placeholder layout — content TBD */}
      </div>
    </section>
  );
}
